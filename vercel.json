{
    "buildCommand": "npm run build",
    "installCommand": "npm install",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ],
    "functions": {
        "*": {
            "memory": 3008,
            "maxDuration": 10,
            "runtime":"@vercel/node"
        }
    }
}